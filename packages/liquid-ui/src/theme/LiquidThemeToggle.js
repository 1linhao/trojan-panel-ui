export const LiquidThemeToggle = { name: 'LiquidThemeToggle', props: { mode: { type: String, default: '' }, label: { type: String, default: 'Toggle color mode' } }, computed: { currentMode() { return this.mode || this.$liquidUI?.theme.getState().mode || 'system' } }, methods: { toggle() { const next = this.$liquidUI?.theme.getState().resolvedMode === 'dark' ? 'light' : 'dark'; this.$liquidUI?.theme.setMode(next); this.$emit('change', next) } }, render(h) { return h('button', { class: 'liquid-theme-toggle', attrs: { type: 'button', 'aria-label': this.label }, on: { click: this.toggle } }, this.currentMode === 'dark' ? '☀' : '☾') } }
