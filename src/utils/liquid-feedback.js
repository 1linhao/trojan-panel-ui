// A single feedback seam keeps page code independent from the current
// notification implementation. Global Liquid styles own the visual contract.
export { Message, MessageBox, Notification } from '@/utils/liquid-feedback'
